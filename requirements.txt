# fastapi==0.115.0 conflicts with mcp==1.0.0 (starlette version clash); using latest compatible
fastapi>=0.115.0
uvicorn[standard]==0.30.6
google-genai==1.0.0
aiosqlite==0.20.0
tomli==2.0.1
pyyaml==6.0.2
# mcp==1.0.0 conflicts with fastapi==0.115.0; using latest compatible
mcp>=1.0.0
# NOTE: the PyPI package is headroom-ai (the `headroom` package is unrelated)
headroom-ai>=0.24.0
python-dotenv>=1.0.0
pytest==8.3.0
pytest-asyncio==0.23.8
httpx==0.27.0
